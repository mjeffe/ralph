services:
  ubuntu-dev:
    build: .
    container_name: ubuntu-dev-container
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true
    env_file:
      - .env
    command: tail -f /dev/null
